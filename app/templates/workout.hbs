{{#if this.showNavbar}}
  <Toolbar
    @title="Workout"
    @tabs={{ array
      (hash label="Programs" route="workout.programs")
      (hash label="History" route="workout.history")
    }}
  />
  {{outlet}}
{{else}}
  {{outlet}}
{{/if}}
