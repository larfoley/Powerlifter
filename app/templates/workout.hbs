{{#if this.showNavbar}}
  <Toolbar
    @title="Workout"
    @tabs={{ array
      (hash label="Diary" route="workout.diary")
      (hash label="My Programs" route="workout.my-programs")
      (hash label="History" route="workout.history")
    }}
  />
  {{outlet}}
{{else}}
  {{outlet}}
{{/if}}
