<Toolbar
  @title={{capitalize @model.name}}
  @backRoute="exercises"
  @tabs={{ array
    (hash label="Records" route="exercises.exercise.records")
    (hash label="Goals" route="exercises.exercise.goals")
    (hash label="Charts" route="exercises.exercise.charts")
  }}
/>
{{outlet}}
